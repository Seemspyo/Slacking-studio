import { NumberSymbolPipe } from './number-symbol.pipe';

describe('NumberSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
